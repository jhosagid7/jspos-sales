<?php require "vendor/autoload.php"; $app = require_once "bootstrap/app.php"; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); $c = App\Models\Customer::where("name", "like", "%ARGENIS MOLINA%")->first(); if($c){ App\Services\CustomerCreditScoringService::evaluate($c); echo "Evaluated. Status: " . $c->fresh()->credit_status; } else { echo "Not found"; }
